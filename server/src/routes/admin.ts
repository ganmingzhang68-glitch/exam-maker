import { Router } from 'express'; import { requireAuth, requireRole } from '../middleware/auth.js'; import { createCostConfig, createUser, dashboard, listAuditLogs, listCostConfigs, listUsers, updateUser } from '../controllers/admin.js';
const router = Router(); router.use(requireAuth, requireRole('admin')); router.get('/dashboard', dashboard); router.get('/users', listUsers); router.post('/users', createUser); router.patch('/users/:id', updateUser); router.get('/audit-logs', listAuditLogs); router.get('/ai-cost-configs', listCostConfigs); router.post('/ai-cost-configs', createCostConfig); export default router;
