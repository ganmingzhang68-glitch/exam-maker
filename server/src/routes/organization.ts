import { Router } from 'express'; import { requireAuth, requireRole } from '../middleware/auth.js'; import { addMember, create, mine } from '../controllers/organization.js'; const router = Router(); router.get('/mine', requireAuth, mine); router.post('/', requireAuth, requireRole('admin'), create); router.post('/:id/members', requireAuth, requireRole('admin'), addMember); export default router;
